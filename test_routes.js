import express from 'express';import resumeRoutes from './resumebuilder/backend/src/routes/resumeRoutes.js';import aiRoutes from './resumebuilder/backend/src/routes/aiRoutes.js';const app = express();app.use('/api/resumes', resumeRoutes);app.use('/api/ai', aiRoutes);const r = [];app._router.stack.forEach(l => {if(l.name==='router'){l.handle.stack.forEach(h => {if(h.route) r.push(l.regexp.toString() + ' ' + h.route.path + ' ' + Object.keys(h.route.methods).join(',').toUpperCase())})}});r.forEach(x => console.log('MOUNT:', x));
